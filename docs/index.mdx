import {
  Root,
} from '../components'

export default props =>
  <Root
    {...props}
    theme={{
      colors: {
        text: 'tomato'
      }
    }}
  />

# hello mdx

This is an mdx file

## beep boop

---

## Notes

- StyleProvider (css vars + type system)
- SCWrapper
