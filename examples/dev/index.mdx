import {
  Head,
} from 'mdx-go'
import {
  Layout,
  NavLinks,
  Pagination,
} from 'mdx-go/components'
import MDXStyle from 'mdx-style'
import { base } from 'mdx-style/themes'
import StyleProvider from '../../components/src/StyleProvider'

<Head>
  <title>Hello</title>
</Head>

export const Root = props =>
  <MDXStyle
    css={{
      ...base
    }}
  >
    <Layout>
      <Layout.MenuToggle />
      <Layout.Sidebar>
        <NavLinks {...props} />
      </Layout.Sidebar>
      <Layout.Main>
        {props.children}
        <Pagination {...props} />
      </Layout.Main>
    </Layout>
  </MDXStyle>

```
```

# Dev Example

This example can be run from the root directory as a development environment for the core library.

- [Typography](/typography)

```.jsx
<h1>Hello</h1>
```

```jsx
import React from 'react'

export default () =>
  <div>
    <h1>Not Live</h1>
  </div>
```
