import {
  Head,
  Layout,
  NavLinks,
  Pagination,
  StyleProvider,
  ComponentProvider,
} from 'mdx-go'

<Head>
  <title>Hello</title>
</Head>

export { DocsLayout as Root } from 'mdx-go'

export const xRoot = props =>
  <StyleProvider
    style={{
      // '--live-border': '#f0f0f6',
      // '--live-editor-background': '#f6f6ff'
    }}>
    <Layout>
      <Layout.MenuToggle />
      <Layout.Sidebar>
        <NavLinks {...props} />
      </Layout.Sidebar>
      <Layout.Main>
        {props.children}
        <Pagination {...props} />
      </Layout.Main>
    </Layout>
  </StyleProvider>

# Dev Example

This example can be run from the root directory as a development environment for the core library.

```.jsx
<h1>Hello</h1>
```
