import {
  Head,
} from 'mdx-go'

<Head>
  <title>Hello</title>
</Head>

export const files = require.context('..', true, /\.example\.mdx?$/, 'lazy')

export const xRoot = props =>
  <div>
    {props.children}
  </div>

# Dev Example

This example can be run from the root directory as a development environment for the core library.

- [Typography](/typography)
- [Hello](/hello)
- [Button](/components/button)


```jsx
import React from 'react'

export default () =>
  <div>
    <h1>Not Live</h1>
  </div>
```
