import { Layout } from 'blazin'

export const Root = props =>
  <Layout>
    <Layout.Sidebar>
      Sidebar
    </Layout.Sidebar>
    <Layout.Main>
      {props.children}
    </Layout.Main>
  </Layout>

# Hello blazin

This is a test fixture for snapshot testing
